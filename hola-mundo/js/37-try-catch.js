'use strict'

try {
    var year = 2019;
    alert(year);
    var vector = new Array(999999999999999999999);

} catch (error) {
    console.log(error);
    alert("Ha ocurrido un error en el código");
}
